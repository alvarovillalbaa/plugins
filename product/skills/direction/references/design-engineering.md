# Design Engineering

External owner boundary:

- Use `hallmark` for UI craft, animation taste, interaction polish, component states, visual hierarchy, typography, tokens, and responsive design gates.

This local reference no longer carries design-engineering doctrine. Keep only repo-specific implementation constraints in `polish/references/ui-constraints.md` and defer visual decisions to Hallmark.

## Local use

Before invoking Hallmark, gather:

- framework and styling stack
- existing motion library
- installed component primitives
- target files and routes
- existing tokens or `design.md`
- whether the user approved broad redesign or only local polish
