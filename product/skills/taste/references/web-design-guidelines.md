# Web Design Guidelines

External owner boundary:

- Use `hallmark audit` for UI/design guideline review.
- Use `hallmark redesign` for approved source edits.

If a project explicitly requires another official guideline source, fetch it live and treat it as an additional constraint. Do not keep a local copy of broad design rules here.
