# Epic Design Examples

External owner boundary:

- Use `hallmark study` when the user provides a reference design.
- Use `hallmark` default or redesign flow when examples are needed to set visual direction.

Do not maintain a local pattern catalog that competes with Hallmark's themes, macrostructures, or DNA extraction.
