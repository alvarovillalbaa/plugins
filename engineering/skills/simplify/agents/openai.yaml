interface:
  display_name: "Simplify"
  short_description: "Reduce code complexity without changing behavior"
  default_prompt: "Use $simplify to reduce duplication, indirection, and unnecessary complexity in the requested scope while preserving behavior and verifying the result."
