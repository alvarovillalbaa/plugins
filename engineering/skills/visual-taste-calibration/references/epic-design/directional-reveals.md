# Directional Reveals

External owner boundary:

- Use `hallmark` for reveal choice, motion restraint, reduced-motion behavior, and anti-slop animation checks.

Keep project-specific animation libraries and performance constraints local.
