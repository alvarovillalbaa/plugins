# Motion System

External owner boundary:

- Use `hallmark` for motion taste, animation purpose, easing discipline, reduced-motion behavior, and interaction-state motion.

Local notes should only name existing project motion libraries or commands.
