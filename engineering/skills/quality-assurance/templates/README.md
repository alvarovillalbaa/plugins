# Templates

- `test-suite-scaffold.md` - the canonical `tests/` tree, per-folder README stubs, `tests/tmp/.gitignore`,
  pytest/Vitest/Playwright runner configuration, and the `tests/README.md` contract table a team fills in
  with its own environment facts.
