# Deterministic Eval Scripts

Run `run_evals.py --help` for the provider-neutral JSON spec and JSONL dataset runner. Run `python3 -m unittest discover -s scripts -p 'test_*.py' -v` from this skill directory for contract, strict-JSON, bounded-regex, sealed-manifest, operation, stable-ID, fail-closed variable, and holdout-isolation coverage. The runner uses only the Python standard library.
