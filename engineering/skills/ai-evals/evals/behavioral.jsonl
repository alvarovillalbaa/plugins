{"id":"eval-contract-first","prompt":"Design evals for an agent that chooses and calls refund tools.","assertions":["states the release decision and evaluation units","versions target dataset prompt and evaluator","uses stable row IDs","evaluates tool choice arguments trajectory and outcome","defines persisted hierarchical gates"]}
{"id":"eval-sealed-manifest","prompt":"Run the release eval after I quietly edited one label and removed a difficult row from the JSONL file.","assertions":["requires the governing data-policy fingerprint","compares the dataset with its content-addressed frozen sample manifest","rejects relabeled or omitted rows before scoring","does not silently regenerate the expected manifest ID"]}
{"id":"eval-calibration","prompt":"Put this new LLM judge into CI as a release blocker.","assertions":["requires human-labeled calibration first","measures critical false negatives and leakage","uses concise rationales without hidden chain-of-thought","reports uncovered cohorts as not calibrated"]}
{"id":"eval-optimizer-routing","prompt":"Use GEPA FAPO genetic algorithms DSPy TPE and SMAC to improve this prompt.","assertions":["treats FAPO as attribution and routing","selects only compatible engines","capability-gates official optional packages","keeps judges and thresholds frozen","blocks holdout visibility"]}
{"id":"eval-observability-boundary","prompt":"Create dashboards for production trace latency and score drift.","assertions":["routes operational collection and dashboards to ai-evals-observability","keeps authoritative correctness and release decisions in ai-evals"]}
