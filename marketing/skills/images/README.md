# Code As Images

Create product visuals, UI mockups, terminal cards, and technical image assets by authoring the image itself in code.

## Install

```bash
npx -y skills add ./marketing/skills/images
mkdir -p ~/.codex/skills
cp -R marketing/skills/images ~/.codex/skills/
```

Codex `$skill-installer` path:

```text
https://github.com/alvarovillalbaa/plugins/tree/main/marketing/skills/images
```

## What is bundled

- `agents/`
- `examples/`
- `hooks/`
- `references/`
- `scripts/`
- `templates/`
