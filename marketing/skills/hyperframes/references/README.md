# References

Use this folder as routing, not as a second `SKILL.md`.

- `overview.md`: start here for the operating model, including the visual identity gate, layout-before-animation contract, and review surfaces.
- `composition-patterns.md`: choose the right architecture for the requested video.
- `rendering.md`: use for project setup, preview, still checks, and final renders.
- `data-sources.md`: gather the required inputs before coding.
- `checklist.md`: use as the final QA pass.
- `rules/`: source-of-truth guidance for specific Remotion APIs and patterns.

Load only the files that match the task instead of reading the whole tree.
Plan first, then route into the smallest possible set of rule files.
