---
title:
audience:
mode:
---

## Build Brief

Implement a presentation that matches the approved outline and style direction.

## Required Behavior

- Keep every slide within the viewport without internal scrolling.
- Use `clamp()` for responsive type and spacing.
- Support local keyboard and on-screen navigation.
- Support `right` or `bottom` navigation placement when deck navigation is present.
- Include remote control only when the delivery context requires it.
- Keep slide compositions intentionally varied across the deck.
- Avoid generic startup defaults: Inter-only typography, indigo accents, gradient-text headings, and repeated centered hero layouts.

## Assets

- Bundled template to start from:
- External assets to preserve:
- Generated assets to create:

## Acceptance Checks

- Style direction is clear and intentional.
- Image sourcing is explicit.
- Mobile, tablet, and desktop layouts remain usable.
- Validation passes for the chosen implementation mode.
- No three consecutive slides use the same composition pattern unless the brief requires a strict template.
