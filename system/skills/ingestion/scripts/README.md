# Scripts

Executable helper scripts owned by this skill.

- `brain_inventory.py` - read-only inventory for `BRAIN.md` boundaries, raw queue source/status counts, Memory candidate counts, and local extraction tool availability.
