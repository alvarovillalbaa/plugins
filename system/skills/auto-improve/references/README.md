# References

- Use `references/docs/source-runtime-skill-model.md` for source/runtime policy.
- Use `references/docs/personalization-policy.md` for overlay-only personalization.
- Use `references/docs/runtime-context.md` for inherited first-use personalization.
- Route explicitly requested canonical source maintenance to `plugins-management`;
  it is outside `auto-improve`.
